website/config.php
config.php
.DS_Store
.vscode/
*.swp

# Generiertes Design-Bundle — wird aus website/index.html gebaut
design/bundle/

# Vendorte Agent-Werkzeuge — gehören nicht in das Trainingsrepo, werden lokal
# bzw. über den jeweiligen Marketplace installiert
.claude/
.github/skills/
.github/agents/
.github/hooks/

# Impeccable: Projektwissen (config.json, critique/) wird versioniert,
# Maschinenzustand nicht
.impeccable/config.local.json
.impeccable/hook.cache.json

# Video-Analyse-Rohdaten (Videos, Frames, Plots) — nie committen, nur
# verdichtete Erkenntnisse wandern manuell nach coach/state.json
coach/video_analysis/

# Trainer 3.0 — Ideation-Arbeitsstand, wandert erst als MVP ins Repo
V3.0/
